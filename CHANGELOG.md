# 変更履歴

コード側の変更だけを記録しています。`data/` の週次自動更新（毎週水・木に GitHub Actions が実行）はここには載せません。

機械的に読める形は [version.json](version.json) にあり、Pages のデプロイ時に自動生成されます。

| 種別 | 最終更新の確認方法 |
|---|---|
| コード | このファイルの最上段、または `version.json` の `code.updatedAt` |
| データ | `version.json` の `data.updatedAt` と `latestMarginWeek` |

---

## 2026-07-31

### デプロイ不具合の修正
- `vercel.json` に入れていた `"//"`（コメントのつもり）がスキーマ違反でビルドを失敗させていた問題を修正。**07-28 以降このサイトの API は本番へ反映されておらず、07-27 時点のコードのまま動いていた**。失敗しても直前の成功デプロイが配信され続けるため気付きにくかった
- あわせて効果のなかった `regions`（東京リージョン指定。無料プランでは反映されない）を削除

### 品質改善（Lighthouse 4項目 100 を実測）
- **セキュリティ**: `innerHTML` に検索語と銘柄名が未エスケープで入りスクリプト注入が可能だった問題を修正。CSP を追加し、API 応答にセキュリティヘッダーを付与
- **アクセシビリティ**: 検索候補がキーボードで選べなかったのを `<button>` 化して修正。ライトテーマのコントラスト不足（15箇所中12箇所が基準未達）を是正。フォーカス表示、`prefers-reduced-motion`、`main`/`label`/`caption`/`aria-live` を追加
- **SEO**: canonical・OGP・Twitter Card・構造化データ・`robots.txt`・`sitemap.xml`・favicon を追加
- API オリジンへ `preconnect`。`server.js` の MIME に `.txt`/`.xml` を追加

## 2026-07-28

- 取得元（Yahoo）のレート制限対策を強化。CDN キャッシュを 1 時間 + `stale-while-revalidate` 7 日に延長し、取得できなかった場合は保存済みデータを `stale` 付きで返すようにした
- 週次更新の取得間隔を 1.5 秒 → 5 秒に

## 2026-07-27

### 画面の刷新
- サイト名を「株信用チェッカー」から **信用残ウォッチ** に変更
- ダーク基調のダッシュボードに刷新。表示テーマを 自動 / ライト / ダーク の3択に（既定ダーク、`localStorage` 保存、`?theme=` でも指定可）
- 検証済みの配色に差し替え、増減は色だけに頼らず ▲▼ を併記。数値を等幅に統一
- 信用倍率にも前週比の ▲▼ を追加
- フッターの出典表記を外し、免責を独立した注記に

### レート制限への耐性
- 429/5xx を指数バックオフで3回まで再試行。「取得元の混雑」と「銘柄が存在しない」を区別
- API がエラーでも保存済みデータへフォールバックするように
- 週次更新に木曜のバックアップ実行を追加

### デプロイ構成の確立
- 取得ロジックを `lib/kabu.js` に共通化し、`api/` をサーバーレス関数として分離
- GitHub Pages（画面）+ Vercel（API）の2段構成に。`config.js` で API の URL を設定
- GitHub Actions で Pages への配信と週次のデータ更新を自動化
